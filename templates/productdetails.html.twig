{% extends 'base.html.twig' %}

{% block title %}
    {{ parent()  }}
    {{ title }}
{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-sm-8 p-1 productsdetails-card">
                <div class="card border-secondary p-0">
                    <div class="card-header">
                        <h5>{{ magazine.product.name }}</h5>
                    </div>
                    <div class="card-body">

                    </div>
                </div>
            </div>
            <div class="col-sm-4 p-1">
                <div class="card border-secondary p-0">
                    <div class="card-header">
                        <h5>{{ magazine.product.name }}</h5>
                    </div>
                    <div class="card-body">

                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}

{% block javascripts %}{% endblock %}